import banner from "../images/banner.png"
import "./index.css"

export const Header = () => {
    return (
        <header>
           <img src={banner} alt="banner"> 
           </img> 
        </header>
    )
}